import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const Timelogs = ({navigation}) => {
  return (
    <View>
      <Text>Time logs</Text>
    </View>
  );
};

export default Timelogs;

const styles = StyleSheet.create({});
