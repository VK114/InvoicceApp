import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import FAB from '../commonscreen/Fab';
const Dashboard = () => {
  return (
    <View
      style={{
        flex: 3,
      }}>
      <FAB />
    </View>
  );
};

export default Dashboard;

const styles = StyleSheet.create({});
