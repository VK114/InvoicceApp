import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const CreditNotes = ({navigation}) => {
  return (
    <View>
      <Text>CreditNotes</Text>
    </View>
  );
};

export default CreditNotes;

const styles = StyleSheet.create({});
